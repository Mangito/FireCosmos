const GConfigs = {
	screen: {
		width: 1000,
		height: 600,
		middleWidth: 0,
		middleHeight: 0,
	},
	debug: false,
};

GConfigs.screen.middleWidth = GConfigs.screen.width / 2;
GConfigs.screen.middleHeight = GConfigs.screen.height / 2;

export default GConfigs;
